
#include <iostream>
using namespace std;

int main(){
    int x, y, z;
    
    cout << "Enter the value of x: ";
    cin >> x;
    
    cout << "Enter the value of x: ";
    cin >> y;
    
     z = ((x * x) - (2 * x * y) + (y * y));
     
     cout  << "answer is: "<<  z;
}